CREATE TYPE PERSON AS OBJECT (
  NAME VARCHAR2(30),
  ADDRESS VARCHAR2(30),
  PHONE NUMBER,
  SSN NUMBER,
)


CREATE TYPE EMPLOYEE UNDER PERSON (
  SALARY NUMBER,
  SHIFT NUMBER,
  WORKS_ON AIRPLANE_TYPE
)

CREATE TYPE PILOT UNDER PERSON (
  RESTR NUMBER,
  LIC_NUM NUMBER,
  FLIES AIRPLANE_TYPE,
)


CREATE TYPE OWNER AS OBJECT (


)

CREATE TYPE CORPORATION AS OBJECT (
  NAME VARCHAR2(30),
  ADDRESS VARCHAR2(30),
  PHONE NUMBER
)
